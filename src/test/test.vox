varas counter func [] do
    varas c 0
    varas count func [] do
        as c (add c 1)
        return c
    end

    return count
end


varas count (counter)

(print (count))
(print (count))
(print (count))
(print (count))
(print (count))